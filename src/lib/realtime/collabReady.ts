/**
 * 주간보고 공동 편집 화면(`/write`)이 준비됐는가.
 *
 * 서버는 공동 편집 주차의 레거시 저장을 막는다(개인 Report 로 취합본을 다시 만들면
 * 팀이 함께 작성한 내용을 덮어쓰기 때문). 그래서 작성 화면이 공동 편집으로 바뀌기 전에
 * 옵션을 켜면 그 팀은 저장할 수단이 아예 없어진다 — 그동안 이 값이 false 로 막고 있었다.
 *
 * `/write` 전환이 끝나 true 로 열었다. 문제가 생기면 다시 false 로 돌리면
 * 켜기만 막히고, 이미 켠 팀은 설정에서 끌 수 있다(끄기는 언제나 허용한다).
 */
export const COLLAB_WRITE_READY = true;
