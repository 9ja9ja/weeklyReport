/**
 * 주간보고 공동 편집 화면(`/write`)이 준비됐는가.
 *
 * 서버는 이미 공동 편집 주차의 레거시 저장을 막는다(개인 Report 로 취합본을 다시 만들면
 * 팀이 함께 작성한 내용을 덮어쓰기 때문). 그래서 **작성 화면이 공동 편집으로 바뀌기 전에**
 * 옵션을 켜면 그 팀은 저장할 수단이 아예 없어진다.
 *
 * `/write` 전환이 끝나면 이 값을 true 로 바꾼다. 그전까지는 켜기를 거부한다.
 * (끄기는 언제나 허용해야 한다 — 잘못 켠 상태에서 빠져나올 길을 막으면 안 된다.)
 */
export const COLLAB_WRITE_READY = false;
